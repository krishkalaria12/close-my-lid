name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: macos-15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Validate Swift package
        working-directory: apps/macos
        run: |
          swift run CloseMyLidCoreTests
          swift build --product CloseMyLid
          swift run CloseMyLid --version
          swift run CloseMyLid status

      - name: Install Raycast dependencies
        working-directory: packages/raycast
        run: npm ci

      - name: Validate Raycast extension
        working-directory: packages/raycast
        run: |
          npm run lint
          npm run build
          npx tsc --noEmit

      - name: Validate Homebrew formula
        run: ruby -c Formula/close-my-lid.rb

      - name: Package macOS app
        run: |
          ./scripts/package-macos-app.sh
          plutil -lint "dist/macos/Close My Lid.app/Contents/Info.plist"
          codesign --verify --deep --strict --verbose=2 "dist/macos/Close My Lid.app"
